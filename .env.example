# Variables d'environnement — copie ce fichier en .env.local et remplis tes clés.
# .env.local n'est jamais commité (.env* est gitignoré).

# --- Backend / scripts (gateway LiteLLM, validate_nvidia_models.py, dev server) ---
# Clé NVIDIA NIM (build.nvidia.com) — cœur des modèles.
NVIDIA_API_KEY=""
# Fallbacks optionnels :
OPENROUTER_API_KEY=""
GEMINI_API_KEY=""
GROQ_API_KEY=""

# --- Frontend (Vite) — laisse vide si tu saisis les clés dans Paramètres > Gateway IA ---
VITE_GEMINI_API_KEY=""
VITE_OPENROUTER_API_KEY=""
